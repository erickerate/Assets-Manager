name: data
description: A library for managing data
version: 1.0.0
publish_to: 'none'
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.4.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  http: ^1.2.2

  domain:
    path: ../domain

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
